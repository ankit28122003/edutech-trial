# Task: Move Pricing & Add Contact Form to Course Detail Page

## Completed Steps
- [x] 1. Create `CourseContactForm.jsx` component
- [x] 2. Edit `CourseDetailPage.jsx` — Replace `CoursePricingCard` with `CourseContactForm` in hero section
- [x] 3. Edit `CourseDetailPage.jsx` — Move `CoursePricingCard` to the bottom section's right column
- [x] 4. Fix mobile menu not closing on navigation (stale closure + Lenis interference)
- [x] 5. Fix Lenis smooth scroll fighting body scroll lock

✅ Build completed successfully with zero errors

