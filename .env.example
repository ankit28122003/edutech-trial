# Base URL of the backend API (Express server). Swap this when the backend is ready.
VITE_API_BASE_URL=http://localhost:5000/api/v1

# Razorpay public key (test key for dev). Never put the secret key in the frontend.
VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
